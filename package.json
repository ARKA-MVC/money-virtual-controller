{
  "name": "money-virtual-controller",
  "version": "1.0.0",
  "description": "A controller for your wallet",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "concurrently \"cd api && npm i\" \"cd client && npm i\"",
    "dev": "concurrently \"cd api && npm run dev\" \"cd client && npm run start\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "dependencies": {
    "chart.js": "^2.9.4",
    "react-chartjs-2": "^2.11.1"
  }
}
